# Rex Seo archive — design entry point

The public archive design contract lives in [the root DESIGN.md](../../DESIGN.md).

`index.html` uses `assets/terminal-home.css`, a dependency-free WebGL system study and a CSS fallback. Submission documents retain the shared document stylesheet and print contracts. This file remains only as the existing design-system entry point; it does not define competing tokens or an older terminal skin.
