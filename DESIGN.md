# Rex Seo — Recoverable by design

서민재의 공개 개발자 포트폴리오다. 채용 담당자는 경력과 성과를 빠르게 읽고, 엔지니어는 각 판단의 근거를 공개 글·코드·커밋에서 확인한다. 홈은 운영 문제와 공개 작업을 연결하는 화면이며, 제출 문서는 읽기와 A4 출력을 위한 별도 형식을 유지한다.

## 디자인 방향

첫 화면의 실제 WebGL 금속 구조는 책임이 나뉜 시스템을 표현한다. 메인 문장과 행동은 구조 바깥에 배치한다. 카메라나 애니메이션을 조작하지 않아도 내용과 링크를 모두 읽을 수 있다. 모형은 개념 표현이며 운영 시스템이나 실시간 상태를 표시하지 않는다.

히어로의 강조는 정체성, 3D 구조, 대표 작업으로 이어지는 버튼에 집중한다. 가짜 터미널 로그와 실행 결과, 반복되는 큰 수치 카드를 제거했다. 내용은 운영 사례 → 경력 → 공개 구현과 기여 → 문서 → 연락 순서로 읽힌다. 공개 프로젝트는 실제 커밋과 코드에 연결하며, 재현·학습 프로젝트를 운영 성과와 구분한다.

## 홈 토큰과 구현

홈 원본은 `index.html`, 스타일은 `assets/terminal-home.css`, 3D는 `assets/system-scene.js`다. 기존 스타일 파일 이름은 자산 배포 호환성을 위해 유지했다. 공유 동작은 `assets/site.js`에 남긴다.

| 의미 | 코드 토큰 | 값 / 사용 |
| --- | --- | --- |
| 기본 배경 | `--term-bg` | `#141612`, 따뜻한 차콜 |
| 보조 표면 | `--term-panel` | `#1b1e18` |
| 본문 | `--term-ink` | `#f1f2e9` |
| 보조 본문 | `--term-muted` | `#b2b8a9` |
| 메타데이터 | `--term-dim` | `#969f8b` |
| 강조·초점 | `--term-green` | `#d4ef9b` |
| 구분선 | `--term-line` | `#34392f` |
| 조작부 경계 | `--term-line-strong` | `#67715b` |
| 긴 사례의 종이 표면 | `--term-paper` | `#f0f0e6` |
| 종이 표면 본문 | `--term-paper-ink` | `#20251b` |

- 한글은 로컬 시스템 sans 계열, 날짜·커밋은 시스템 monospace를 쓴다. 외부 폰트를 요청하지 않는다.
- 제목은 크기와 간격으로 구분한다. 대다수 정보는 구분선과 행으로 묶고, 도식과 실제 문서 선택에만 패널을 쓴다.
- 데스크톱 내용 너비는 최대 1280px. 760px 이하에서는 20px 여백, 360px 이하에서는 16px 여백이다.
- 본문은 기본 16px이며 사례 요약은 13–14px, 메타데이터는 9–11px다. 모바일의 긴 한글은 단어 단위로 줄바꿈한다.
- 홈의 섹션은 처음부터 보인다. 스크롤 진입 애니메이션으로 본문을 감추지 않는다.

## 인터랙션과 대체 화면

- 섹션 이동과 문서/PDF 링크는 일반 HTML 링크다. GitHub Pages 하위 경로에서도 동작하도록 로컬 주소는 상대 경로다.
- 3D는 정점·법선·조명을 직접 처리하는 WebGL 1이다. 외부 모델, 이미지 텍스처, 런타임 라이브러리가 없다.
- 구조·데이터·복구 버튼은 시점과 레이어 간격을 바꾸며 선택 상태를 `aria-pressed`로 표시한다. 정지 버튼으로 회전을 멈출 수 있다.
- `prefers-reduced-motion`에서는 정적인 프레임만 그린다. 화면 밖이나 숨겨진 탭에서는 렌더 루프를 멈춘다. 픽셀 비율은 최대 1.7이다.
- WebGL 초기화 실패·컨텍스트 손실·JavaScript 비활성화에서는 CSS 3D 대체 구조와 본문이 남는다. 사용 불가능한 조작부는 숨긴다.
- 키보드 초점은 2px 외곽선으로 표시한다. 본문 건너뛰기 링크는 `tabindex="-1"`인 `main`으로 이동한다.
- 이메일 복사가 거부되면 기존 복사 방식으로 재시도하고, 그것도 실패하면 주소를 표시한다. 임시 입력 요소를 제거한 뒤 버튼으로 초점을 되돌린다.

## 경력 내용의 근거

`design/public-evidence.md`에 공개 원문·커밋과 주장 범위를 기록한다. 통계에는 확인 날짜와 공개 API 범위를 붙인다. 인증된 GitHub 권한으로 볼 수 있는 비공개 이력은 공개 업적으로 추가하지 않는다. JPA 약 200만 건 보정과 Boot 전환 테스트 160개 이상은 각각 다른 성과다. Spring 기여는 PR API 상태에 맞게 병합 또는 제안 반영으로 표시한다.

## 제출 문서의 기존 계약

`resume/`, `career/`, `portfolio/`는 `assets/design-system.css`의 A4 인쇄 계약을 유지한다. 화면에서는 파일 끝의 `@media screen` 규칙으로 홈과 같은 올리브 헤더와 종이색 본문을 적용한다. 핵심 본문은 16px이며, 중첩된 문서 외곽 장식과 모바일의 중복 문서 링크를 줄였다. 홈은 이 불변 템플릿에서 제외한다. 원본 프로필 사진과 데이터 편집 ID, 관리자 검증, PDF 다운로드는 유지한다.

| 제출 원본 | 변경 전 SHA-256 기준 |
| --- | --- |
| `resume/index.html` | `a8e8d4c10c8513d2da5c2a84ce10bb01d1a28f4b68a7310935eae6310d4e333b` |
| `career/index.html` | `2901a8115d1b7ff2a91d38a05604215a1c592798472b19a46031c81162889977` |
| `portfolio/index.html` | `ed341baa65b4b3d22d3e773c4da20be48baa24331bc410d330d88268d26a85b4` |

인쇄 기준은 이력서 1쪽, 경력기술서 2쪽, 포트폴리오 3쪽이다. 화면 변경은 `@media screen` 범위로 제한하고 PDF 출력 CSS와 분리한다. 브랜드 헤더는 `screen-only`이며, 본문 건너뛰기 대상에는 `tabindex="-1"`을 둔다. 기존 인쇄 규칙과 제출 PDF 파일은 수정하지 않았다. 기존 문서의 디자인 출처는 Supabase 공개 디자인 기록(2026-05-17, `4482a96f9c1f2426ab08c570f86c84ee045534c8`)이며 제휴 관계를 뜻하지 않는다.

## 검증

`ai-slop-remover`의 진단 → 시각·동작·문구 개선 → 실제 브라우저 검증 절차를 적용했다. 반복마다 화면을 직접 열어 확인하고 `.omx/state/3d-archive/ralph-progress.json`에 관찰과 다음 수정 범위를 남긴다.

- `npm test`: 편집·문서·관리자 회귀와 WebGL 생명주기 검사.
- `npm run lint`, `npm run typecheck`, `npm run build`.
- `node scripts/check-home-browser.mjs <origin> output/screenshots/<run> --full-page`: 실제 Chrome의 1440·1280·390·320px, 확대에 해당하는 좁은 레이아웃, 링크·복사·모션·대체 화면 확인.
- 브라우저 실행 증거와 이미지의 사람/에이전트 시각 검수를 별도로 기록한다. 실제 모바일 GPU의 배터리·장시간 성능은 측정하지 않았다.
