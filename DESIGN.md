# Rex Seo — 백엔드 엔지니어 이력서와 작업 기록

서민재의 공개 개발자 이력서와 포트폴리오다. 채용 담당자는 소개·기술·경력·성과를 차례로 읽고, 엔지니어는 각 사례의 근거를 공개 글·코드·커밋에서 확인한다. 홈은 화면에서 읽는 이력서이며, 제출 문서는 읽기와 A4 출력을 위한 별도 형식을 유지한다.

## 2026-09-07 — Connected project universe (current home)

사용자는 우주 분위기가 배너에서 끝나지 않고 하단 전체로 이어져야 하며, 업적 그래프와 기존 흐름도를 행성 간 연결로 표현해 달라고 요청했다. 제공한 어벤져스 포스터의 깊이·명암·서로 겹치는 구성에서 출발하되, 실제 경력과 구현 흐름을 시각화한다.

- **연속 공간:** `.stellar-backdrop`의 별, 화면 밖까지 이어지는 큰 행성, 긴 궤도선이 작업·경력·공개 기록·문서·연락처까지 이어진다. 밝은 종이 바탕은 제거했다. 본문은 불투명한 `#0b1722` 읽기 영역 위에 밝은 글자로 표시한다.
- **읽기 순서:** 포스터 소개 → 현재 하는 일·기술 → 프로젝트 행성 지도·성과·처리 흐름 → 경력 궤도 → 공개 코드·글 → 문서 → 연락처. 그래프를 긴 경력 목록보다 먼저 접한다.
- **행성 선택:** 네 행성이 기존 네 프로젝트 탭의 실제 버튼이다. 동일한 `assets/resume-explorer.js` 상태 모델로 선택·키보드·직접 링크·JavaScript 없는 읽기를 유지한다. 선택한 행성에서 본문 방향으로 연결선이 강조된다. 지도는 서로 독립된 프로젝트의 탐색이며 시스템 의존성을 뜻하지 않는다.
- **처리 흐름:** 네 `assets/diagrams/orbit-*.svg`가 실제 인과와 분기를 나타낸다. 피드의 Snapshot Hit/Empty 분기, FPR·SPR·Re-Rank, 캐시 우회 경로를 보존했다. 기존 `feed-serving.svg`는 검색 사례에서 펼쳐 볼 수 있다. [사실 매핑](design/orbit-flow-notes.md)을 따른다.
- **업적 그래프:** JPA 약 200만 건 보정은 총량으로 표시한다. 별도 Spring Boot 전환의 테스트 0→160개 이상만 비교 막대다. 이벤트 9종·상태 6개·복구 8경로와 API 9개·동시성 13시나리오는 서로 다른 단위이므로 독립 수치로 표시한다. 향상률이나 성공률을 만들지 않는다.
- **모바일:** 행성 지도는 2×2이고, 선택된 내용을 아래에서 읽는다. 복잡한 SVG는 키보드 접근 가능한 가로 스크롤 영역이며 완전한 한국어 흐름 요약과 세로 행성 목록을 함께 둔다. 본문 글자를 줄여 도식을 끼워 넣지 않는다. 헤더 PDF는 유지한다.
- **인쇄:** 배경 우주와 선택 지도는 제외하고 모든 사례·수치·흐름도를 표시한다. 별도 제출 문서와 PDF는 A4 형식을 유지한다.
- **모션:** 연속 애니메이션은 없다. 행성 선택의 테두리와 위치만 짧게 반응하며 모션 감소 설정에서는 이동을 없앤다.

| 역할 | 현재 홈 코드 토큰 / 선택자 | 값 / 사용 |
| --- | --- | --- |
| 우주 바탕 | `--paper`, `.universe-shell` | `#080d14` / 하단 전체의 연속된 암부 |
| 읽기 표면 | `[data-reading-surface]`, `.case-panel` | `#0b1722` / 우주 장식을 본문 뒤에서 차단 |
| 본문 | `--ink` | `#eff3f5` |
| 보조 설명 | `--muted` | `#b8c4cd` |
| 선택·링크 | `--accent` | `#e9bc79` / 금빛 궤도와 연결 |
| 구분선 | `--line` | `#344450` |
| 이름 타이틀 | `.cosmic-title h1` | 로컬 Barlow Condensed Black Italic·금속 효과 |
| 프로젝트 지도 | `.planet-navigator`, `.world-sphere` | 질감 있는 구체, SVG 궤도선, 기존 탭 관계 |
| 흐름도 | `.planet-flow`, `.flow-viewport` | 1000×420 네 SVG, 모바일 가로 스크롤·텍스트 요약 |
| 경력 | `.career-list` | 행성 노드와 연결선, 실제 기간·회사·업무 |

홈은 `assets/universe.css` 한 장을 사용한다. 아트워크는 기존 [우주 장면](design/cosmic-artwork.md)을 재사용하며 새 그래프는 코드로 작성한 SVG다. 현재 브라우저 검증은 `scripts/check-universe-browser.mjs`, 자료는 `output/universe-qa-v3/`에 남긴다. `validate:content`는 HEAD 대비 문구만 수정하는 작업용이므로 전면 구조 개편에는 적용하지 않는다. 아래는 이전 터미널 디자인의 기록이다.

## 이전 디자인 방향

최신 요청은 “더 interactive 하고 우아하고 멋지게, AI 같지 않게”이며 한국어 윤문 스킬 사용을 포함한다. 앞서 요청한 넓은 이력서, 원본 사진이 들어간 입체 터미널, 아래로 이어지는 명령 출력은 유지하면서 실제 내용을 제자리에서 탐색할 수 있도록 바꾼다. 사용자가 제시한 [이선우 소개 페이지](https://snwlee.github.io/intro/)에서는 화면 전체로 열린 첫 화면, 제목과 읽기 순서, 간결한 탐색을 참고했다. 기준 화면과 계산된 스타일은 `output/screenshots/resume-reference/`에 남아 있다.

`.terminal-shell`은 화면 전체 너비이고 `main`은 최대 1480px다. 데스크톱 헤더는 이름, 소개·경력·작업·기록 탐색, PDF·명령어 조작을 한 줄에 놓는다. 첫 화면에서는 큰 한국어 이름과 같은 기준선의 이탤릭체 `Rex Seo`, 현재 하는 일, 소속과 재직 기간을 먼저 읽는다. PDF 링크는 밑줄과 작은 원형 화살표로 표시한다. 오른쪽 사진 터미널은 소개·기술·연락처를 바꿔 보는 실제 읽기 도구다. 이름과 주요 소개는 회전하는 면 바깥에 두어 같은 위치에서 읽을 수 있게 한다.

전체 배경은 푸른 기가 약한 차콜, 본문은 따뜻한 흰색, 선택과 초점은 채도를 낮춘 연두색이다. 구분선과 여백으로 정보의 덩어리를 나눈다. 한국어는 산세리프, 영문 이름과 절 번호는 Georgia 이탤릭, 명령·날짜·파일 정보는 모노스페이스를 사용한다. 화면 위계를 만드는 두 조작 대상은 사진 터미널과 작업 사례 선택 영역이다.

읽는 순서는 소개·기술 → `career` → `work` → `activity` → `docs` → `view system` → `contact`다. 경력은 기간·회사·설명을 나란히 배치한다. 소개 아래 기술 요약은 기존 이력서의 사실을 `백엔드`, `데이터 · 연동`, `테스트 · CI`로 나눈다. 공개 활동에서는 Spring 기여와 글·발표를 먼저 두고 공개 프로젝트를 이어 읽는다. 넓은 화면에서 두 묶음은 좌우 열을 사용한다.

작업 영역은 `JPA 데이터 보정`, `이벤트 복구`, `배치·동시성`, `검색·캐시` 네 항목과 선택한 사례의 본문·작업 과정·출처로 구성한다. 데스크톱은 왼쪽 세로 목록과 오른쪽 상세 내용, 800px 이하에서는 2×2 목록 아래 상세 내용으로 바뀐다. JPA 약 200만 건 보정과 Boot 전환 테스트 160개 이상은 각각 다른 작업으로 설명한다. 수치와 공개 근거는 해당 사례 안에 두고, 학습·재현 프로젝트와 오픈소스 병합·제안 반영의 범위를 유지한다.

사진 터미널에는 원본 `assets/profile.jpg` 한 장을 두고 그 옆의 내용을 `프로필`·`기술`·`연락처` 탭으로 바꾼다. 사진과 얼굴은 변경하지 않는다. 연락처 탭의 메일·GitHub·웹 이력서는 실제 링크다. 이 탭은 아래 명령 입력으로 이동하지 않으며, 하단 명령 입력은 독립적으로 계속 사용할 수 있다.

한국어는 포괄적인 자기평가보다 맡은 일과 확인한 결과를 쓴다. 소개는 현재의 이벤트 복구·개인화 피드 개발을 설명하고, 경력·사례는 문제와 조치를 짧은 문장으로 연결한다. 변경 예와 사실 범위, 런타임 안내는 [한국어 윤문 기록](design/korean-copy-review.md)에 남긴다.

## 홈 토큰과 구현

홈 원본은 `index.html`, 스타일은 `assets/terminal-home.css`다. `assets/resume-explorer.js`가 소개와 작업 사례의 탭을 각각 초기화한다. 소개의 입체 동작은 `assets/portrait-terminal.js`, 하단 WebGL은 `assets/system-scene.js`, 명령 실행은 `assets/archive-terminal.js`가 담당한다. 공유 탐색·복사 동작은 `assets/site.js`에 남긴다.

| 의미 | 코드 토큰 | 값 / 사용 |
| --- | --- | --- |
| 전체 배경 | `--term-bg` | `#121415` |
| 상태 알림 배경 | `--term-panel` | `#1b1e20` |
| 본문 | `--term-ink` | `#f0ede4` |
| 보조 본문 | `--term-muted` | `#bdc1b8` |
| 날짜·출처 | `--term-dim` | `#9da69a` |
| 선택·링크·초점 | `--term-green` | `#c6d59b` |
| 구분선 | `--term-line` | `#353938` |
| 조작부 경계·링크 밑줄 | `--term-line-strong` | `#777b70` |
| 명령 입력 배경·강조 영역 글자 | `--term-inset` | `#0b0d0e` |

색상과 산세리프·모노스페이스 글꼴 토큰은 `assets/terminal-home.css`의 `:root`에서 정의한다. 외부 폰트는 요청하지 않는다. 제출 문서는 아래의 기존 A4 계약을 따른다.

| 역할 | 실제 선택자 | 현재 화면의 배치 |
| --- | --- | --- |
| 넓은 이력서와 헤더 | `.terminal-shell`, `main`, `.site-header`, `.brand`, `.header-actions` | 본문 최대 1480px·좌우 48px. 데스크톱은 74px 높이의 한 줄 고정 헤더이며, 현재 탐색 위치는 2px 밑줄로 표시 |
| 소개 이름과 행동 | `.hero`, `.hero h1`, `.hero h1 span`, `.hero-description`, `.resume-download` | 소개와 400–530px 사진 열, 열 간격 72px. 한국어 이름 `clamp(62px, 6vw, 86px)`, 같은 줄의 영문 이름은 29px Georgia 이탤릭. 소개 20px, PDF 링크 최소 높이 48px |
| 사진의 입체 장면 | `[data-portrait-root]`, `.portrait-stage`, `.portrait-device`, `.portrait-face`, `.portrait-side` | 원근 1450px·두께 18px. 기본 X -5°·Y -10° 회전은 `--portrait-rx`·`--portrait-ry`로 적용. 터치 세로 스크롤과 확대 제스처를 유지 |
| 사진과 내용 선택 | `.profile-tabs`, `.portrait-profile`, `.profile-output`, `.profile-readout`, `[data-explorer-panel]` | 사진과 내용은 1 / 1.1 두 열. 원본 비율 689/886·사진 Z 26px·본문 Z 9px·탭 Z 40px로 조작을 사진 앞에 둠. 프로필·기술·연락처 내용은 같은 자리에서 전환 |
| 시점과 움직임 | `.portrait-controls`, `[data-portrait-view]`, `[data-portrait-toggle]` | 회전하는 장면 바깥의 입체·정면 보기와 움직임 버튼. 선택 상태는 색상·밑줄·`aria-pressed`로 표시 |
| 기술과 경력 | `.resume-skills`, `.career-list`, `.career-date`, `.career-company` | 기술 요약은 세 열. 1100px 이상에서 경력은 기간 170px·회사 245px·설명 세 열과 34px 간격. 회사명 22px·설명 17px |
| 절 제목 | `.command-line`, `.section-heading`, `.section-index` | 작은 `$ career` 형태의 명령 다음에 절 번호·32px 한국어 제목. 반복 사용자명과 경로는 화면에서 숨겨 본문 폭을 확보 |
| 작업 선택과 상세 | `.case-explorer`, `.case-tabs`, `.case-panels`, `.case-panel`, `.case-flow` | 초기화 후 300px 세로 목록과 상세 내용을 64px 간격으로 배치. 선택한 사례의 제목은 29px, 본문은 16px·최대 64ch. 과정은 세 항목의 사실 목록, 근거는 일반 링크 |
| 공개 활동과 문서 | `.public-notes`, `.repository-list`, `.documents`, `.document-no` | 1100px 이상에서는 기여·글과 공개 저장소를 두 열로 배치. 문서는 디렉터리·제목·설명·웹/PDF 링크가 이어지는 행 |
| 보조 모형 | `.hero-visual`, `.system-scene`, `.scene-toolbar`, `.scene-caption` | 최대 440px 너비·220px 높이의 WebGL 스케치. `[data-renderer="webgl"]`이 있을 때 조작줄을 표시하고 대체 구조에서는 숨김. 시점과 층 간격을 바꾸는 모형이며 실제 시스템 실행을 나타내지 않음 |
| 이어지는 명령 입력 | `.terminal-session`, `.terminal-output`, `.terminal-entry`, `.terminal-form` | 연락처 아래의 폼과 누적 결과. 결과 영역에 별도 스크롤이나 최대 높이를 두지 않고 입력 줄이 페이지 아래로 이어짐 |

- 1100px 이하에서는 사진 열을 350–450px, 열 간격 32px로 줄인다. 이름은 64px·영문 이름 25px·소개 18px다. 작업 선택 열은 240px, 상세와 간격은 40px다.
- 800px 이하에서는 본문 좌우 여백 24px, 소개와 사진은 한 열로 바뀌며 사진 열의 최대 너비는 520px다. 헤더는 이름·탐색 아래에 PDF·명령어 줄을 둔다. 작업 목록은 2×2로 놓고 상세 내용을 아래로 보낸다.
- 560px 이하에서는 본문 좌우 여백 20px, 58px 높이의 한 줄 헤더를 사용한다. 헤더의 중복 PDF 링크와 단축키 표시는 숨기고 소개의 PDF 링크는 유지한다. 이름은 50px·영문 이름 24px·소개 16px다. 사진 원근은 1000px·깊이는 18px이며 기술 요약과 경력 날짜는 한 열로 읽는다. 사례 제목은 23px·본문은 15px다.
- 원문과 명령 결과는 페이지 자체의 세로 스크롤을 사용한다. 탭 전환은 160ms 동안 5px 이동과 투명도 변화로 선택한 내용만 보여 주며, 모션 감소에서는 이 효과와 부드러운 스크롤을 끈다. 읽기 시작을 지연하는 타이핑이나 진입 연출은 없다.
- 홈 편집 ID 25개와 각 요소 타입을 유지한다. 제출 문서의 편집 필드 160개, HTML, A4 인쇄 규칙과 PDF 파일은 이번 홈 개편에서 변경하지 않는다.

## 인터랙션과 대체 화면

- 섹션 이동과 문서/PDF는 일반 HTML 링크다. 로컬 주소는 상대 경로를 유지해 하위 경로에서도 사용할 수 있게 한다. 본문 건너뛰기 링크는 `tabindex="-1"`인 `main`으로 이동한다.
- 두 `[data-explorer]`는 서로의 탭과 내용을 제어하지 않는다. 초기화 전에 키·ID·기본 선택·`aria-controls`·버튼과 내용의 대응을 검증한다. 구성이 유효할 때만 `tablist`·`tab`·`tabpanel` 관계와 선택 상태를 부여하고 선택하지 않은 내용을 숨긴다. JavaScript가 없거나 검증에 실패하면 모든 원문과 링크가 그대로 남고 조작 탭은 숨긴다.
- 소개 탭의 기본값은 `프로필`, 작업 탭의 기본값은 `JPA 데이터 보정`이다. 선택 탭에만 `tabindex="0"`을 주고 본문에는 초점을 둘 수 있게 한다. 클릭·방향키·Home·End로 바꿀 수 있으며, 방향키로 선택할 때 초점도 함께 이동한다. 한글 조합이나 보조키를 누른 입력은 가로채지 않는다.
- 사례 선택은 `#case-jpa`, `#case-event`, `#case-batch`, `#case-search`와 사례 안의 `#impact` 같은 세부 대상에 대응한다. 직접 해시 주소로 들어오거나 해시가 바뀌면 대상을 포함한 사례를 표시한다. 숨겨져 있던 대상의 해시로 이동하면 먼저 사례를 연 뒤 해당 위치로 스크롤한다. 탭 선택 자체는 `history.replaceState`로 주소만 맞추며 선택할 때마다 방문 기록을 추가하거나 다른 페이지 위치로 이동하지 않는다. 소개 탭은 주소를 바꾸지 않는다.
- 소개의 CSS 3D 장면은 실제 HTML·원본 사진을 `preserve-3d`와 `translateZ`로 배치한다. 마우스처럼 정확한 포인터와 hover가 가능한 환경에서만 기울기를 따라 움직인다. 링크·탭 위나 누르는 동안에는 움직임을 멈춰 클릭 대상을 유지한다. 키보드로 내부에 초점이 들어오면 정면으로 고정하고 초점이 나가면 선택한 시점으로 돌아간다.
- 입체·정면 보기와 움직임 켜기·멈추기는 장면 밖에서 조작한다. 모션 감소에서는 정면으로 시작하고 포인터 추적·보간을 끈다. `동작 줄이기 켜짐`으로 설정 상태를 표시하며, 포인터 추적을 지원하지 않는 환경에서는 `시점 버튼으로 조작`을 안내한다. 화면 밖·숨겨진 탭에서는 프레임을 멈추고 목표 시점에 도착해도 루프를 종료한다. JavaScript가 없으면 정적인 입체 사진과 전체 프로필 내용이 남는다.
- 하단의 명령 입력은 연락처 다음에 있다. 헤더의 `명령어` 버튼과 Cmd/Ctrl+K로 폼에 즉시 초점을 옮긴다. 결과를 입력 줄 위에 추가하고 실행 후 초점을 유지하며, 작성 중인 초안과 오류 입력을 보존한다. 폼에는 화면 끝과의 여백 16px을 둔다.
- `help`·`ls`·`top`·`work`·`career`·`activity`·`docs`·`contact`·`view system/data/recovery`·`pause`·`resume`·`clear`를 지원한다. 도움말의 명령은 버튼으로도 실행한다. 원문 요약과 관련 링크를 결과에 추가하고 `career`에는 회사·재직 기간·서비스를 포함한다. 사진 터미널의 탭 선택은 이 명령 실행과 별개다.
- 입력 이력과 출력은 현재 페이지 세션의 최근 30개만 유지한다. `clear`는 실행해 쌓인 출력만 지우고 정적 내용은 유지한다. 위아래 방향키로 이력과 초안을 찾고, Tab은 후보가 하나일 때만 완성한다. 한글 조합 중에는 명령과 단축키를 실행하지 않는다. 출력은 `role="log"`에 한 항목씩 추가하며 텍스트·허용된 링크만 옮기고 편집 ID나 조작부는 복제하지 않는다.
- 하단 WebGL 1 스케치는 외부 모델·텍스처·런타임 라이브러리를 사용하지 않는다. 구조·데이터·복구 시점과 `view` 명령은 회전값과 층 간격을 바꾼다. 캡션은 `세 층으로 나눈 시스템 구성`, `층 사이를 넓힌 안쪽 구조`, `층 사이를 좁힌 반대쪽 구조`처럼 실제 보이는 변화를 설명한다. 움직임 버튼과 `pause`·`resume`은 모형의 움직임을 제어한다.
- WebGL은 모션 감소에서 정적 프레임만 그리고 화면 밖·숨겨진 탭에서는 루프를 멈춘다. 픽셀 비율은 최대 1.7이다. 초기화 실패·컨텍스트 손실·JavaScript 비활성화에서는 CSS 대체 구조와 본문이 남는다. 사용할 수 없는 조작은 숨기거나 비활성 상태로 표시한다.
- 홈 인쇄에서는 선택하지 않은 작업 사례도 모두 표시한다. 탭·사진 조작·헤더·명령 입력 등 화면용 요소는 숨기고 사례는 배경과 최소 높이를 제거한 일반 본문으로 출력한다. 제출 PDF의 A4 규칙은 별도로 유지한다.
- 링크·버튼의 키보드 초점은 2px 외곽선, 명령 입력은 강조색 밑줄로 표시한다. 이메일 복사가 거부되면 기존 복사 방식으로 재시도하고, 그것도 실패하면 주소를 표시한다. 임시 입력 요소를 제거한 뒤 버튼으로 초점을 되돌린다.

## 경력 내용의 근거

`design/public-evidence.md`에 공개 원문·커밋과 주장 범위를 기록한다. 통계에는 확인 날짜와 공개 API 범위를 붙인다. 인증된 GitHub 권한으로 볼 수 있는 비공개 이력은 공개 업적으로 추가하지 않는다. JPA 약 200만 건 보정과 Boot 전환 테스트 160개 이상은 각각 다른 성과다. Spring 기여는 PR API 상태에 맞게 병합 또는 제안 반영으로 표시한다.

## 제출 문서의 기존 계약

`resume/`, `career/`, `portfolio/`는 `assets/design-system.css`의 A4 인쇄 계약을 유지한다. 화면에서는 파일 끝의 기존 문서 `@media screen` 블록으로 기존 올리브 헤더와 종이색 본문을 적용한다. 앞선 제출 문서 개편은 이 화면 블록 안에서만 이루어졌으며 문서 HTML은 바꾸지 않았다. 이번 홈의 인터랙션·한국어 개편에서는 제출 문서와 이 화면 스타일을 변경하지 않는다. 핵심 본문은 16px이고 원본 프로필 사진, 데이터 편집 ID, 관리자 검증, PDF 다운로드를 유지한다. 홈은 이 제출 템플릿에서 제외한다.

제출 문서는 이어 읽는 종이 표면으로 정리한다. 이력서의 기술 표, 경력기술서의 경력 목록과 대표 사례에서 어두운 패널과 중첩 외곽선을 줄였다. 포트폴리오의 소개·프로필·사례·공개 기록도 카드 테두리 대신 간격과 구분선으로 나눈다. 모바일에서 중복되는 문서 링크를 숨겼던 기존 결정은 유지한다.

| 문서 화면 역할 | 실제 토큰·선택자 | 공통 결정과 화면 예외 |
| --- | --- | --- |
| 읽기 표면과 본문 | `--sb-canvas`, `--sb-ink`, `--sb-muted`, `.resume-capability-list`, `.career-overview`, `.career-project`, `.case-section` | 종이색 `#f0f0e6`과 본문색 `#20251b`를 유지. 기술·경력·사례를 연속된 행으로 표현하며 `#jpa`도 같은 읽기 표면 사용 |
| 프로필과 소개 | `.profile-portrait`, `.portfolio-profile`, `.portfolio-title` | 사진의 중첩 테두리를 제거. 포트폴리오 제목은 36–46px, 700px 이하에서는 32px. 프로필은 큰 어두운 카드 대신 구분선으로 묶음 |
| 작업 목차 | `.portfolio-layout`, `.case-nav`, `.case-nav a[aria-current="true"]` | 1000px 초과에서는 192px의 단순한 텍스트 목차. 1000px 이하에서는 가로 목차로 전환. 선택 상태는 `--sb-green-ink` 선으로 표시하고 이동 대상이 고정 목차 아래에 오도록 여백 확보 |
| 사례의 비교 정보 | `.fact-grid`, `.fact-cell`, `.scope-item` | 데스크톱은 2열 사실 목록. 700px 이하에서는 항목명 64px과 내용을 나란히 놓은 세로 행. 공개 기록은 카드 외곽선 없이 구분선으로 묶음 |

| 제출 원본              | 초기 개편 당시 기록한 SHA-256                                      |
| ---------------------- | ------------------------------------------------------------------ |
| `resume/index.html`    | `a8e8d4c10c8513d2da5c2a84ce10bb01d1a28f4b68a7310935eae6310d4e333b` |
| `career/index.html`    | `2901a8115d1b7ff2a91d38a05604215a1c592798472b19a46031c81162889977` |
| `portfolio/index.html` | `ed341baa65b4b3d22d3e773c4da20be48baa24331bc410d330d88268d26a85b4` |

위 해시는 초기 개편의 이력이다. 현재 제출 원본은 Git HEAD와 일치하며, 최신 해시와 편집 필드 수는 `output/screenshots/resume-wide/build-validation.md`에 별도로 기록한다.

인쇄 기준은 이력서 1쪽, 경력기술서 2쪽, 포트폴리오 3쪽이다. 화면 변경은 `@media screen` 범위로 제한하고 PDF 출력 CSS와 분리한다. 브랜드 헤더는 `screen-only`이며, 본문 건너뛰기 대상에는 `tabindex="-1"`을 둔다. 기존 인쇄 규칙과 제출 PDF 파일은 수정하지 않았다. 기존 문서의 디자인 출처는 Supabase 공개 디자인 기록(2026-05-17, `4482a96f9c1f2426ab08c570f86c84ee045534c8`)이며 제휴 관계를 뜻하지 않는다.

## 검증

프로젝트 스킬은 [Lutriva 2.2.0](https://github.com/ch4570/lutriva/tree/69149804ea292a8a8589c9dde0ebf164d0dde8fc)의 7개 모음 전체다. `.agents/skills/`에 설치했고 55개 원본 파일의 해시를 확인했다. 출처와 호출 방법은 `.agents/README.md`, 원본 커밋과 파일 해시는 `.agents/lutriva-source.json`에 기록한다. 최신 지침의 디자인 기록·실제 토큰 연결과 과업별 패턴 선택을 적용했다.

### 현재 소개·사례 인터랙션과 한국어 개편 — 2026-09-07

넓은 이력서에서 소개·기술·연락처와 네 작업 사례를 제자리에서 탐색할 수 있도록 바꿨다. 헤더·타이포그래피·색상·PDF 링크를 정리하고 한국어 소개·경력·사례·런타임 안내를 윤문했다. 구현 매핑은 현재 `index.html`, `assets/terminal-home.css`, `assets/resume-explorer.js`를 읽어 기록했다. 이전 개편의 검사 수는 이번 검증에 합산하지 않는다.

- 최종 전체 `npm test` 104개(스크립트 92개·TypeScript 12개), lint·typecheck·production build가 통과했다. 빌드는 정적 페이지 10개를 생성했다. 실행 로그는 `output/screenshots/editorial/tests-final.log`·`lint-final.log`·`typecheck-final.log`·`build-final.log`에 있다.
- 한국어 문구 수정 직후 기존 문구 검사 12개가 통과했고, HTML의 편집 ID·요소 타입 25개와 문구 외 태그·속성을 보존했다. 런타임 문구 변경 후 모형 집중 검사 16개와 해당 파일 ESLint가 통과했다. 이 소스 보존 검사는 텍스트 수정 단계의 것이며 이후 의도적으로 바꾼 홈 구조 전체의 불변성을 주장하지 않는다.
- 실제 데스크톱·모바일 첫 화면과 JPA 사례 화면을 구현 담당이 열어 확인했다. 현재 화면과 중간 브라우저 근거는 `output/screenshots/editorial/`에 있다. 기준 진단과 개편 전 화면은 `output/screenshots/editorial-before/`, 윤문 근거는 `design/korean-copy-review.md`에 남긴다.
- 사진 앞에 탭의 깊이 면을 두어 실제 클릭 대상을 확보했고, 사례 해시가 숨긴 본문 안을 가리킬 때 대상을 연 뒤 즉시 이동하도록 수정했다. 실제 포인터로 프로필·기술·연락처를 선택한 독립 재검사 52개가 여섯 너비에서 통과했다. 근거는 `output/screenshots/editorial/profile-final/browser.json`이며, 데스크톱·모바일 연락처 화면도 구현 담당이 직접 확인했다. 연속 해시 이동 복구를 포함한 사례 선택 단위 검사 12개가 통과했다.
- 최종 소스에서 홈 편집 ID·요소 타입 25개와 편집 내용 검증, 원본·공개 자산 7개의 일치를 확인했다. 제출 문서 세 개는 HEAD 대비 변경이 없고 편집 필드 160개를 유지한다. 근거는 `output/screenshots/editorial/build-validation.json`과 `document-contracts.log`다.
- 기존 홈 브라우저 검사 80개 중 79개가 통과했다. 한 항목은 인쇄 검사가 이전 사례 선택자를 사용해 실패했으며, 실제 `.case-panel`을 검사하도록 고친 뒤 인쇄 집중 검사 2개가 통과했다. 모든 사례의 본문 표시와 표본 대비, 명령 조작부 숨김을 확인했다. 근거는 `output/screenshots/editorial/home-final/browser.json`과 `print-final/browser.json`이다. 전체 80개를 다시 실행한 결과로 합산하지 않는다.
- WebGL 준비 상태에서 하단 조작줄을 표시하는 CSS 규칙을 복원했다. 독립 브라우저 검사 168개 중 165개가 통과했고, 빠른 해시 이동에서 발생한 세 실패는 숨긴 대상을 연 뒤 즉시 스크롤하도록 수정해 해시 집중 검사 14개로 재확인했다. 근거는 `output/screenshots/editorial/final/browser.json`과 `hash-final/browser.json`이다. 기존 전체 검사를 재실행한 것으로 합산하지 않는다.
- 독립 QA의 현재 범위 판정은 `pass`다. 1440·1920·1024·720·390·320px 배치, 실제 포인터·터치 이벤트, 키보드, 직접 링크·뒤로/앞으로 이동, 모션 감소·JavaScript 대체 화면, 터미널·문서 링크·인쇄를 확인했다. 구현 담당과 QA 모두 실제 캡처를 열어 소개·경력·사례·연락처·명령 출력의 읽기 순서와 줄바꿈을 확인했다. 범위와 제약은 `output/screenshots/editorial/visual-review.md`에 기록한다.
- 제출 문서 HTML·A4 인쇄 규칙·원본 사진·제출 PDF는 이번 작업에서 변경하지 않았다. 물리 모바일 기기·장시간 성능·운영 배포는 검사하지 않았다.

### 이전 이력서 중심의 넓은 홈 개편 — 2026-09-07

이력서에 맞게 화면 폭과 타이포그래피를 넓히고, 소개·기술·경력·성과 순서로 정리했다. 원본 사진의 입체 터미널과 아래로 이어지는 명령 입력 동작은 유지한다. 아래의 이전 검사는 이번 레이아웃의 검증으로 합산하지 않는다.

- 구현과 문서의 현재 연결은 `index.html`, `assets/terminal-home.css`를 기준으로 기록했다. 기준 사이트의 캡처와 계산된 스타일은 `output/screenshots/resume-reference/`에 있다.
- 최종 테스트 92개(스크립트 80개·TypeScript 12개), lint·typecheck·production build가 통과했다. 홈 편집 ID·태그 25개 보존과 CSS 파싱, 준비된 공개 자산의 원본 일치를 확인했다. 제출 문서 HTML 세 개는 Git HEAD와 같고 편집 필드 160개를 유지한다. 실행 근거는 `output/screenshots/resume-wide/build-validation.md`에 있다.
- 1920·1440·1024·800·390·320px와 확대에 해당하는 720px 재배치 등 초기 브라우저 검사 94개 중 93개가 통과했다. `career` 명령에서 회사·기간이 빠진 기존 요약을 보완한 뒤 연속 출력·경력 맥락·초안·모바일 배치 재검사 7개가 통과했다. 최초 결과는 `output/screenshots/resume-wide/first-pass/browser.json`, 수정 후 검사는 `output/screenshots/resume-wide/career-final/browser.json`에 보존한다.
- 최신 소스의 기존 홈 전체 브라우저 회귀 검사 80개가 모두 통과했다. 탐색·입력·문서/PDF 응답·키보드·모션 감소·대체 화면과 홈 인쇄 표시를 확인했으며 근거는 `output/screenshots/resume-wide/home-regression/browser.json`이다.
- 구현 담당과 독립 QA가 데스크톱·모바일의 소개와 경력, 사진 시점·모션 감소·JavaScript 대체 화면·명령 출력을 직접 확인했다. 검수 범위와 판정은 `output/screenshots/resume-wide/visual-review.md`에 기록한다. 물리 모바일 기기·장시간 성능·운영 배포는 검사하지 않았다. 제출 PDF는 재생성하지 않았다.

### 이전 사진 입체 터미널 개편 — 2026-09-07

첫 소개에 원본 사진을 담은 CSS 입체 터미널과 시점·움직임 조작을 추가했다. 아래의 이전 검사 기록은 이번 구현의 검증으로 합산하지 않는다.

- `node --test scripts/archive-terminal.test.mjs`의 15개 검사가 통과했다. 이번에 추가한 검사는 소개 바로가기의 실제 요약·링크 출력, 입력 초안과 이력 복구, 하단 입력 초점과 즉시 스크롤, 명령 버튼의 범위 제한을 확인한다.
- 최종 전체 테스트 90개(스크립트 78개·TypeScript 12개), lint·typecheck·production build가 통과했다. 입체 동작 검사 8개에는 페이지의 초점 가능한 상위 요소가 마우스 추적을 막지 않는 회귀 검사가 포함된다. 홈의 편집 ID와 요소 타입 25개를 유지하고, 준비된 공개 자산이 원본과 같은지 확인했다. 실행 근거는 `output/screenshots/portrait-terminal-final/build-validation.md`에 기록한다.
- 실제 Chrome의 입체 소개 검사 45개가 통과했다. 1440·1280·720·390·320px 배치, 실제 마우스 회전과 복귀, 정면·입체 선택, 정지, 키보드 초점, 터치 스크롤, 모션 감소, JavaScript 대체 화면과 바로가기 명령을 확인했다. 최종 실행 근거는 `output/screenshots/portrait-terminal-final/browser.json`이다. 초기 실행에서 찾은 마우스 추적 결함을 수정했고, 실패 기록은 `output/screenshots/portrait-terminal/`에 별도로 보존한다.
- 기존 홈 전체 브라우저 검사는 80개 중 78개가 통과했고, 모바일 두 너비에서 입력 줄이 화면 아래로 약 0.5px 걸리는 문제가 있었다. 입력 폼에 스크롤 여백 16px을 적용한 뒤 390·320px의 입력 버튼·Cmd+K·명령 실행·가로 배치 검사 10개가 모두 통과했다. 최초 전체 실행은 `output/screenshots/portrait-home-regression/browser.json`, 수정 후 재검사는 `output/screenshots/portrait-prompt-final/browser.json`에 남긴다. 이를 전체 80개 재실행 통과로 표시하지 않는다.
- 구현 담당과 독립 QA가 실제 캡처를 직접 확인했다. 첫 입체 화면, 기울임·정면·모션 감소, 390·320px, JavaScript 대체 화면과 입력 줄을 검수했다. 시각 판정과 범위는 `output/screenshots/portrait-terminal-final/visual-review.md`에 기록한다.
- 제출 문서·인쇄 규칙·원본 사진은 이번 입체 소개 변경에서 수정하지 않았다. 모바일은 Chrome 터치 에뮬레이션으로 확인했으며 물리 기기, 장시간 성능과 운영 배포는 검사하지 않았다.

### 이전 연속 터미널 개편 — 2026-09-07

사용자의 후속 요청에 따라 홈을 연속된 터미널로 다시 구성했다. 현재 소스에서 홈의 편집 ID 25개와 요소 타입 보존을 확인했다. 아래의 이전 구현 결과를 현재 화면의 검증으로 합산하지 않는다.

- 최종 전체 테스트 80개와 lint·typecheck·production build가 통과했다. 인라인 터미널 테스트 13개에는 출력 누적·초점 도착·문서 요약과 실제 다운로드 링크·오류 복구 검증이 포함된다.
- 실제 Chrome 검사 80개가 통과했다. 연속 출력, 입력 줄로의 실제 화면 이동, 키보드 조작, 반응형 배치, 문서/PDF 링크와 대체 화면을 확인했다. 근거는 `output/screenshots/terminal-final/browser.json`에 있다. 화면은 구현 담당과 독립 QA가 직접 확인했으며 시각 검수는 `output/screenshots/terminal-final/visual-review.md`에 기록한다.
- 마지막 문서 요약·다운로드 링크·입력 위치·CSS 대체 구조 재검사 14개가 통과했다. 데스크톱과 320px에서 제목·설명만 출력되고 링크가 유지되며 대체 구조가 잘리지 않는지 확인했다. 근거는 `output/screenshots/terminal-polish/browser.json`과 같은 경로의 캡처다.
- 제출 문서와 A4 계약은 이번 터미널 개편에서 변경하지 않았다. 제출 PDF를 재생성하지 않았으며, 물리 모바일 기기·장시간 GPU 성능·운영 배포는 검사하지 않았다.

### 이전 소개 중심 개편 — 2026-09-07

앞선 화면 개편은 `ai-slop-remover`의 진단 → 시각·동작·문구 개선 → 실제 브라우저 검증 절차를 적용했다. 홈 기준 화면은 `output/screenshots/overhaul-before/`, 중간 검사는 `output/screenshots/overhaul-intermediate/`, 당시 완성된 구현의 기본 검사는 `output/screenshots/overhaul-after/`에 있다. 이 기록은 현재의 연속 터미널 변경 전 상태를 검증한 것이다.

- 당시 홈의 Chrome 기본 검사 78개가 통과했다. 가로 넘침, 문서/PDF 링크, 터미널 탐색과 초점 복귀, 이메일 복사 대체 경로, 모션 감소, JavaScript/WebGL 대체 화면을 확인했다. 터미널의 3D 이동 대상은 `#system-sketch`이며, 결과는 `output/screenshots/overhaul-after/browser.json`에 있다. 중간 구현의 78개 통과 기록은 별도 폴더에 보관한다.
- 당시 홈 데스크톱·390px·320px 화면과 제출 문서의 포트폴리오 데스크톱·경력기술서 390px·이력서 데스크톱을 직접 열어 확인했다. 독립 QA의 추가 경계 너비 검사 15개와 사례 제목 링크 4개의 존재 검사가 통과했다. 스크롤이 끝난 뒤 사례 제목을 실제로 클릭하여 네 목적지의 주소와 노출된 제목을 확인했고, PDF의 HTTP 200 응답·콘텐츠 유형·파일 서명도 검증했다. 해당 재검사 8개는 `output/screenshots/overhaul-links-settled/browser.json`, 터미널의 안정된 초점과 화면 내 배치 재검사 2개는 `output/screenshots/overhaul-terminal-settled/browser.json`에 있다. 당시 검수 범위에서 남은 중대 문제는 없었다.
- `output/screenshots/overhaul-extra/browser.json`의 초기 추가 실행은 경계 너비·링크 존재 검사 뒤 제목 클릭 단계에서 중단됐다. 이 실행 전체를 통과로 표시하지 않으며, 실제 클릭과 도착 확인은 위의 스크롤 안정화 후 재검사 결과를 근거로 사용한다.
- 제출 문서는 1440·390·320px의 세 경로에서 56개 검사가 통과했다. 포트폴리오의 761·1000·1001px 경계 검사 21개도 통과했다. 변경 후 화면 9개를 직접 확인했으며 근거와 범위는 `output/screenshots/overhaul-docs-after/VERDICT.md`와 `output/screenshots/overhaul-docs-breakpoints/`에 있다.
- 제출 문서 3개의 인쇄 미디어 레이아웃·타이포그래피 스냅샷이 개편 전과 일치했다. `assets/design-system.css`의 기존 문서 화면 블록 앞부분은 인쇄 규칙을 포함해 HEAD와 바이트 단위로 같고, 변경한 규칙은 모두 `@media screen` 안에 있음을 CSS 파서로 확인했다. 제출 PDF는 응답만 확인했으며 재생성하거나 새 PDF의 페이지 수를 검사하지 않았다.
- 테스트 80개, lint, typecheck, production build가 통과했다. 홈의 편집 ID 25개와 요소 타입을 유지했고, 제출 문서의 편집 필드 160개는 HEAD 대비 변경이 없다. 홈은 구조 개편이므로 편집 영역 내용만의 차이를 요구하는 검사를 대신해 ID·요소 타입 보존과 실제 동작을 확인했다. 실행 기록은 `output/screenshots/overhaul-after/build-validation.md`에 있다.
- 물리 모바일 기기, 장시간 GPU 성능과 운영 배포는 검사하지 않았다. 이메일 복사 실패는 브라우저 API 주입으로 확인했다.

### 개편 이전 검증 이력

아래는 소개 중심 개편보다 앞서 수행한 개선 기록이다. 당시 첫 화면의 3D, 독립 성과 목록과 탐색 바를 검증한 근거이며, 현재 구조에 대한 새 검사로 합산하지 않는다.

이전 스킬 모음 적용 후 성과 목록을 320·390·720·761·1440px에서 다시 검수했다. 추가 브라우저 검사 30개가 통과했고 5개 화면을 직접 확인했다. 당시 숫자·단위가 같은 줄에 남으며 근거 링크 3개의 목적지·44px 조작 높이를 유지했다. 공개 기여 링크는 실제 클릭 후 `#open-source` 도착과 제목 노출까지 확인했다. 근거는 `output/screenshots/lutriva-bundle/VERDICT.md`와 같은 경로의 `impact-after/`에 있다.

앞선 2026-09-07 개선에도 `ai-slop-remover`를 적용했다. 해당 단계의 로컬 기준 화면은 `output/screenshots/slop-before/`, 변경 후 화면과 실행 결과는 `output/screenshots/slop-after/`에 남아 있다. 데스크톱·모바일 히어로, 작업, 문서 목록과 전체 화면을 직접 열어 확인했다.

- 당시 변경 후 Chrome 검사 76개가 통과했다. 1440·1280·720·390·320px의 가로 넘침, 문서/PDF 링크, 터미널 탐색과 초점 복귀, 이메일 복사 대체 경로, 모션 감소, JavaScript/WebGL 대체 화면을 확인했다. 복사 실패는 브라우저 API 주입으로 검사했다.
- 추가 시각 검수에서 찾은 761px의 3D 시점 버튼 줄바꿈을 수정했다. 조작부는 공간이 부족하면 안내 문구를 다음 행으로 넘긴다. 761·390·320px의 일반/모션 감소 상태 재검사 30개가 통과했으며 화면을 직접 확인했다. 추가 근거는 `output/screenshots/slop-intermediate/`에 있다.
- 당시 기존 테스트 80개, lint, typecheck, production build가 통과했다. 실행 환경은 Node 26.8.1이며 프로젝트의 지정 버전은 Node 24.x였다.
- 당시 홈의 편집 ID 25개와 요소 타입을 보존했다. 제출 문서 3개의 편집 필드 160개는 HEAD 대비 변경이 없으며 `validate-editables`를 통과했다. 홈은 의도적인 구조 변경이므로 HEAD와의 편집 전용 차이 검사 대상에서 제외하고, 기존 테스트로 새 마크업과 편집 영역을 검증했다.
- 기준 화면은 정적 서버, 변경 후 동작 검사는 Next.js 로컬 서버에서 수행했다. 기준 실행의 PDF 404 두 항목은 정적 서버의 경로 매핑 차이이며, 변경 후에는 정상 응답을 확인했다. 물리 모바일 기기 및 운영 배포는 검사하지 않았다.

### 재검증 명령과 범위

- `npm test`: 편집·문서·관리자 회귀, 소개·작업 탭의 선택·키보드·해시 이동, 두 모형의 생명주기, 터미널 탐색·실패 복구·입력 이력 검사.
- `npm run lint`, `npm run typecheck`, `npm run build`.
- `node scripts/check-home-browser.mjs <origin> output/screenshots/<run> --full-page`: 실제 Chrome의 1440·1280·390·320px, 확대에 해당하는 좁은 레이아웃, 링크·복사·모션·대체 화면 확인.
- 브라우저 실행 증거와 이미지의 사람/에이전트 시각 검수를 별도로 기록한다. 실제 모바일 GPU의 배터리·장시간 성능은 측정하지 않았다.

## 공유와 지속 검증

`design/social-card.html`은 1200×630 공유 이미지 원본이다. `scripts/render-social-card.mjs`가 설치된 Chrome에서 실제 WebGL 장면을 모션 감소 상태로 렌더링해 `assets/social-card.png`를 만든다. 네 공개 문서가 이 이미지와 각자의 canonical URL을 사용한다.

PR CI는 Ubuntu 24.04에 설치된 Chrome에서 홈·문서를 검사한다. GPU가 없는 러너에서는 `ARCHIVE_SOFTWARE_WEBGL=1`로 SwiftShader를 명시하고 WebGL 초기화 성공을 필수로 확인한다. 제품의 렌더러는 이 설정을 사용하지 않는다. 검사 후 스크린샷·JSON·서버 로그를 보관하며, 업로드 성공을 시각 검수 완료로 간주하지 않는다. 서로 다른 OS의 글꼴 차이 때문에 로컬 인쇄 레이아웃 스냅샷은 Linux CI의 픽셀 기준으로 사용하지 않는다.
